# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.vite
*.local

# Local env files (keep .env.example tracked)
.env
.env.*
!.env.example

# Vercel
.vercel

# Local KV backups / data dumps (contain customer PII — never commit)
backups/

# Local agent tooling (skills installed via npx skills — not project code)
.agents/
.claude/
skills-lock.json

# Unused video alternates (site serves mp4; webm not referenced)
public/videos/*.webm

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env*.local
public/images/categories/*.jpeg
public/images/categories/*.jpg
.env*

# Local pre-optimize video backups (never commit)
public/videos/aliona-*.orig.mp4
